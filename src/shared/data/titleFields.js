import columns from '../columns';

const titleFields = [
  columns.title,
  columns.imprint,
  columns.pagination,
  columns.isbn,
  columns.location,
  columns.date,
  columns.source,
  columns.cost,
  columns.accessionNumber,
];

export default titleFields;
